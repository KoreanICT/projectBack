<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.ictedu.projectBack.dao.member.MemberDao">
	<insert id="insertMember" parameterType="mvo">
		INSERT INTO member (
			id, pwd, name, nick, email, mphone, storecode, storeaddr, logintype, authority
		) VALUES (
			#{id}, #{pwd}, #{name}, #{nick}, #{email}, #{mphone}, #{storecode}, #{storeaddr}, #{logintype}
			, #{authority},sysdate
		)
	</insert>
	<select id="countByEmail" resultType="int" parameterType="string" >
	select count(*) from member where email = #{email}
	</select>
</mapper>