<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.ictedu.projectBack.dao.survey.ISurveyDao">

	<!--관리자 평가지 등록 매퍼 쿼리문-->
	<insert id="insertSurvey" parameterType="surveyvo">
		INSERT INTO SURVEY VALUES(survey_seq.nextVal,#{code},#{sub},sysdate)
	</insert>

	<!--관리자 평가지 평가 항목 등록 매퍼 쿼리문-->
	<insert id="insertQuestions" parameterType="java.util.List">
		INSERT ALL
		<foreach collection="list" item="item">
			INTO SURVEY_QUESTIONS (SVNUM, QUESTIONS_ID, QUESTIONS_TEXT)
			VALUES (SURVEY_SEQ.CURRVAL, #{item.questions_id}, #{item.questions_text})
		</foreach>
		SELECT * FROM DUAL
	</insert>
	
	<!--평점 등록 매퍼 쿼리문-->
	<insert id="insertRatings" parameterType="map">
		INSERT ALL
		<foreach collection="list" item="item">
			INTO SURVEY_RESULT (MNUM, SVNUM, QUESTIONS_ID, RATING, REQUEST, RESDATE)
			VALUES (#{MNUM}, #{SVNUM}, #{item.questions_id}, #{item.rating}, #{REQUEST}, SYSDATE)
		</foreach>
		SELECT 1 FROM DUAL
	</insert>
	
	<!--최신 평가지 출력 매퍼 쿼리문-->
	<select id="selectSurvey" resultType="surveyvo">
		SELECT SVNUM, CODE, SUB, SDATE FROM SURVEY
		WHERE SVNUM = (SELECT MAX(SVNUM) FROM SURVEY)
	</select>
	
	<!--최신 평가지의 평가 항목 출력 매퍼 쿼리문-->
	<select id="selectQuestions" parameterType="int" resultType="sqvo">
		SELECT QUESTIONS_TEXT FROM SURVEY_QUESTIONS
		WHERE SVNUM = #{SVNUM}
		ORDER BY QUESTIONS_ID
	</select>
	
	<!--평가지 별, 질문 하나 당 평균 출력 매퍼 쿼리문-->
	<select id="selectAverages" parameterType="int" resultType="map">
		SELECT QUESTIONS_ID, AVG(RATING) AS AVG FROM SURVEY_RESULT
		WHERE QUESTIONS_ID IN 
		<foreach collection="list" item="item" open="(" separator="," close=")">
			 #{item.questions_id}
		</foreach>
		AND SVNUM = #{SVNUM} GROUP BY QUESTIONS_ID
	</select>
	
	<!--평가지 삭제 매퍼 쿼리문-->
	<delete id="deleteSurvey">
		DELETE FROM SURVEY
		WHERE SVNUM = (SELECT MIN(SVNUM) FROM SURVEY) PURGE
	</delete>

</mapper>